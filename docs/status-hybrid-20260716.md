# 项目状态看板 — 混合方案（Hybrid：保留 main 公式 + 换 MinerU 图）

> 最后更新：2026-07-16 | PM：牧羊羊（Project Shepherd）
> 前情：原 `mineru` 全量重建分支已 **park**（未完成工作存于 `stash@{0}`，作回退/对照）。本看板跟踪新方向。

## 1. 方向决策（用户拍板）
- **混合策略**：保留 `main` 原始手写版**文本 + 公式 + `:label:`/`{eq}` 交叉引用**（一字不动），只把 figure 二进制换成 MinerU 提取的高清图。
- **动机**：绕开 `mineru` 全量重建踩到的交叉引用 off-by-one 高危区；只做 MinerU 真正强的事（图提取）。
- **铁律**：绝不碰 `main`；新工作在 `img-optimize` 分支（从 main 拉）；未提交；推送前用户批准。

## 2. 分支与资产
| 项 | 状态 |
|---|---|
| `main` | 原始手写版，**只读不动** |
| `img-optimize`（当前） | 从 main 拉，已换 p3-ch03 七图，**未提交** |
| `mineru`（park） | 全量重建，存 `stash@{0}` 作回退，可恢复 |
| MinerU 解析图 | 工作树 `mineru/output/` = PART III（592 张哈希 jpg）；各分册解析在 `stash@{0}^3` 未跟踪父节点：`output_p1`/`p1b`/`p1_merged`、`output_p2`、`output_p4`（正提取到工作树） |

## 3. p3-ch03 试点结果（GO，待你 eyeball）
构建 EXIT=0、0 undefined、0 duplicate；服务已起，7 图全部有效并已正确 serve。

| K | 原图尺寸 | MinerU 版尺寸 | 体积Δ | 备注 |
|---|---|---|---|---|
| 1 | 585×306 (17KB) | **359×273** (27KB) | +55% | ⚠️ 宽缩 39%（线稿可能偏糊） |
| 2 | 601×489 (31KB) | 601×495 (70KB) | +125% | 尺寸近同，更清晰 |
| 3 | 601×777 (93KB) | 596×773 (209KB) | +126% | 尺寸近同，更清晰 |
| 4 | 1390×334 (16KB) | **1112×351** (37KB) | +136% | ⚠️ 宽缩 20% |
| 5 | 501×901 (36KB) | 496×898 (88KB) | +143% | 尺寸近同，更清晰 |
| 6 | 1390×417 (32KB) | **806×417** (74KB) | +131% | ⚠️ 宽缩 42%（横图被压窄） |
| 7 | 1000×520 (67KB) | 992×506 (148KB) | +120% | 尺寸近同，更清晰 |

**画质反直觉点（PM 预警）**：MinerU 对照片/位图（K2/3/5/7）明显更清晰；但对线稿/矢量示意图（K1/4/6）反而**被缩小、可能更糊**——"MinerU 图一定更优"的假设不成立。需你逐张 eyeball 定夺。

**→ 按 (B) 质量优先（用户 2026-07-16 19:36 选定）**：照片类换 MinerU 版、线稿留原图。自动化闸门（已校正）：**MinerU 面积 ≥ 原图 95% 才换**。注：原始 JPG 体积因格式更高效反而比原 PNG 小，故"体积≥1.25x"判据无效，已弃用（详见第 7 节）。

## 4. 决策门（你拍板后全站推广）
- **(A) 全换**：一致性优先，所有图换 MinerU 版（含被压窄的线稿）
- **(B) 质量优先**：只换比原图好的（照片类），线稿留原图
- **(C) 放弃混合**：保留 main 原图
- ✅ **2026-07-16 19:36 用户选定 (B) 质量优先** — 只换比原图好的（照片类），线稿留原图。

## 5. 任务清单
- [x] p3-ch03 可行性 PoC（只读）：GO
- [x] 分支落地（img-optimize / park mineru）
- [x] p3-ch03 试点构建 + 服务 + 对比表
- [x] **你 eyeball p3-ch03 七图 → 定全站策略 = (B) 质量优先** ✅ 2026-07-16 19:36
- [ ] **全站推广（B 策略）按 p3→p4→p1→p2**：每 part 构建闸门 + 引用前缀审计（公式不动，引用风险≈0）← 进行中
- [ ] 验收 + 推送（用户批准）

## 6. 预览
- 试点页：http://localhost:3000/p3-ch03-diamagnetism-paramagnetism-bound
- 首页：http://localhost:3000/

## 7. 推广进度（B 策略）
- **p3（8 章，76 图）**：✅ 已落地（面积≥95% 闸门）。
  - **实际结果**：**42 张换 MinerU / 34 张留原图**。ch03 K2/3/5/7 换、K1/4/6 留（与预期一致）；留原图含 14 多面板 + 2 无匹配 + 18 线稿降采样。换上的 PNG 体积均为原图 1.68–3.04×，机制正常。
  - 异常（留原图=安全，暂缓）：12 张多面板图、ch07-15 孤儿图、ch07 一处未匹配图。
  - 构建 EXIT=0、0 undefined；服务三路由 200。决策明细：`mineru/p3_quality_decision_table.log`。
- **p2（Optical，13 章）**：✅ 已落地（校正脚本重跑）。124 图里 **57 换 / 67 留**；ch13 已补（2 换）。多面板 23 张、无匹配 4 张留原图待 eyeball。
- **p1（Transport，10 章）**：🔧 **已二次修正（2026-07-16 晚）**。发现 p1 全部原图都是 **1700 宽整页渲染**（面积闸门逆向保留了坏图）。推翻面积闸门，凡 MinerU 提取到的图一律换 MinerU 干净裁切：**95 图里 71 换 / 24 留**（ch01=8/1, ch02=16/7, ch03=3/0, ch04=8/2, ch05=7/1, ch06=9/2, ch09=11/7, ch10=9/4；ch07/08 无图）。留的 24 张 = 多面板图（怕 MinerU 拆多张丢面板）+ 2 张 MinerU 未提取（fig-p1-ch01-9 / 子图 1.1/1.9 正确保留）。PM 独立核实：首图均非 1700、子图未误配、ch09-2 多面板已正确 revert 回原图。
- **p4（Superconducting，4 章）**：✅ 已落地。38 图里 **13 换 / 25 留**。
- 📊 **全站合计（git 真相，2026-07-17 复核）**：仓库 `chapters/images/` 共 **470 张图**，**183 张已换 MinerU（p1:71 / p2:57 / p3:42 / p4:13）/ 287 张留原图**。注：原"120/213"系陈旧日志口径，已更正；其中 **137 张 MinerU 从未输出**（直接留原图），不在 333 张"已考虑"范围。0 损坏。

## 8. 整站交付就绪（2026-07-16 晚）
- ✅ **整站最终构建**：`myst build` EXIT=0、0 undefined/error/duplicate、46 页全建。公式/`:label:`/`{eq}` 一字未动。
- ✅ **服务修复（重要运维教训）**：p4 偶发 HTTP 000 的根因 = **端口 3000 上同时堆了 5 个 `myst start` 进程抢端口**（多 Agent 各自起重服务累积）。Server Ops Agent 杀掉全部 5 个僵尸、干净重建，现仅 **1 个服务、四册路由全 200**。
  - ⚠️ **铁律追加**：起重调试服务前**必须先 kill 既有 3000 监听进程**，且全项目**只保留 1 个实例**；禁止多 Agent 并发 `myst start`。今后服务异常优先查「是否多个进程抢端口」。
- ✅ 预览已稳定：http://localhost:3000/ （四册首页均可达）。

## 9. 待用户验收 + 推送
- **你逐册浏览器验收**（四册图渲染）：换上的照片类是否更清晰、留原图的线稿是否 OK。
- **验收 OK → 我派 DevOps Agent 分批 commit/push `img-optimize`**（推送前你批准；独立 `git ls-remote` 核实远端）。
- push 后为非阻断项开 cleanup 工单：多面板图（留原图）、孤儿图（ch07-15、p1 两张 `fig-p1-ch01-1.1/1.9.png`）、各册少量无匹配 MinerU 图。

## 10. 复盘核验（2026-07-17 PM 牧羊羊）
- 详报见 `docs/retrospective-20260717.md`。
- **已证实绿区**：公式/引用零改动（`git diff main -- chapters/*.md`=0）、构建 EXIT=0/0 warning、183 换图 git 可证。
- **账本更正**：183 换 / 287 留（非 120/213）；137 张 MinerU 从未处理。决策日志（`p*_quality_decision_table.log`）跨轮陈旧、不可信，以 `git diff main -- chapters/images/` 为唯一计数源。
- **待核查隐患**：H1 漏换图（p3 ch07-15、p1 md707-719 区间孤儿）、H2 假阴性（0.95 闸门卡掉 87–94.9% 线稿可换）、H3 尚未人眼验收 183 换图、H4 多面板64+1700整页64 留原图。
- **更优管线（网络调研）**：S1 = MinerU 只做定位(middle.json bbox) + PyMuPDF/pdf2svg 从源 PDF 按 bbox 抽（线稿→SVG、照片→高DPI PNG/JPG），可同时升级 183+287；S4 Real-ESRGAN 仅兜底低位照片（绝不用线稿，会幻觉）。推送前就"是否采用 S1"征求用户一次意见。
- 今日**不推送**（用户指令）。
